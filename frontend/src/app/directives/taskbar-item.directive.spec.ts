import { TaskbarItemDirective } from './taskbar-item.directive';

describe('TaskbarItemDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskbarItemDirective();
    expect(directive).toBeTruthy();
  });
});
